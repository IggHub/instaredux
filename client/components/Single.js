import React from 'react';

const Single = React.createClass({
  render() {
    return(
      <div className="single-photo">
        single photo inside component
      </div>
    )
  }
})

export default Single;
